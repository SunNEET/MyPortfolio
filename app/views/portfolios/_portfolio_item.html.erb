<div class="col-md-4">
	<div class="card mb-4 box-shadow" data-id="<%= item.id %>">
		<%= image_tag portfolio_img(item.thumb_image, 'thumb'), class: "card-img-top"%>
		<div class="card-body">
		  <p class="card-text">
		  	<span><%= link_to item.title, portfolio_path(item.id) %></span>
		  	<%= item.subtitle %>
		  </p>
		</div>
	</div>
</div>